.key Switch1/S,Arg1/K,ArgX/M
.bra {
.ket }
; $VER: WHDLoad-Cleanup 1.0 (26.06.26)    
;

If $IPStack EQ Miami
   IF EXISTS ENV:ConnectionType
      SYS:Rexxc/rx SYS:PisSorm/Network/NetworkMiami.rexx Action=connect Device=$ConnectionType KeepEnvStatus SilentRunning
   ENDIF
ENDIF

If $IPStack EQ Roadshow
   IF EXISTS ENV:ConnectionType
      SYS:Rexxc/rx SYS:PisSorm/Network/NetworkRoadshow.rexx Action=connect Device=$ConnectionType KeepEnvStatus SilentRunning
   ENDIF
ENDIF

