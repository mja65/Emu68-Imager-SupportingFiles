.key Switch1/S,Arg1/K,ArgX/M
.bra {
.ket }
; $VER: WHDLoad-Cleanup 1.0 (26.06.26)    
;

IF EXISTS ENV:IPStack
   IF EXISTS ENV:ConnectionType
      SYS:Rexxc/rx SYS:pistorm/network/network.rexx Action=connect IPStack=$IPStack Device=$ConnectionType KeepEnvStatus SilentRunning
	ENDIF
ENDIF

